/* 장바구니 목록 불러오기 */
/*
	
const user_no = document.getElementById("user_no").value;
console.log(user_no);

var total = 0;

$.get('/list/cart', {user_no: user_no})
 .done(function(data){
	data.forEach(function(item){
		$(".price").append(item.game_price + "원");
		$(".game_title").append(item.game_title);
		$(".game_title").val(item.game_title);
		$(".game_content").append(item.game_content);
		$(".game_level").append("Level:" + item.game_target_level);
		$(".game_discount_price").append("할인율:" + item.game_discount_price);
		total += item.game_price;
	})
		$("#total_price").html(total+"원");
		$(".total_price").html(total+"원");
		
		console.log( $(".game_title").val() );
		
 })
 .fail(function(error){
	 console.log("실패")
 });

*/

 







